import { HttpMethod } from '../common';
export declare type CommandType = 'external-api';
export declare type CommandCallback = (orgId: string, userId: string, data: any) => any;
export interface CommandOptions {
    /** create an action for this command to be used in workflows */
    action?: boolean;
    /** what the command does in as few words as possible */
    description?: string;
}
export interface Command {
    type: CommandType;
    name: string;
    displayName?: string;
    permittedArgs?: string[];
    url: string;
    httpMethod: HttpMethod;
    defaultHeaders?: any;
    defaultBody?: any;
}
