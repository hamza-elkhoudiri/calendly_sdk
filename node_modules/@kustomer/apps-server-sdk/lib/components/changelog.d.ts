/**
 * A key value pair where the version describes changes that have happened to components installed by the app over time. The date is required to be between {{DATE}}. The rest can just be markdown.
 * e.g. "2.0.0": "{{April 2021}} New Feature \n-   Added a new feature")
 */
export interface Changelog {
    readonly [version: string]: string;
}
