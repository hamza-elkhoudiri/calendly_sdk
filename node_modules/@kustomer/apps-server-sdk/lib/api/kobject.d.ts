import { WithID } from '../common';
import { KObject } from '../models';
import { APIBase, APIBaseOptions } from './base';
export declare class KObjects extends APIBase {
    constructor(token: string, orgName: string, options: APIBaseOptions);
    /**
     * get a customer by id
     * @param id customer id (unique)
     */
    getByExternalId(externalId: string, klass: string): Promise<WithID<KObject> | undefined>;
}
